// DO NOT EDIT! This file was auto-generated by crates/build/dl_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/dl_sdk_types/definitions/dalaran/components/voxel_size.fbs".

#pragma once

#include "../datatypes/vec3d.hpp"
#include "../result.hpp"

#include <array>
#include <cstdint>
#include <memory>

namespace dalaran::components {
    /// **Component**: The scene-unit dimensions of one voxel in a sparse 3D voxel grid.
    ///
    /// Each component is the size of a voxel along the corresponding local grid axis.
    /// All components must be finite and positive.
    struct VoxelSize {
        dalaran::datatypes::Vec3D xyz;

      public:
        VoxelSize() = default;

        VoxelSize(dalaran::datatypes::Vec3D xyz_) : xyz(xyz_) {}

        VoxelSize& operator=(dalaran::datatypes::Vec3D xyz_) {
            xyz = xyz_;
            return *this;
        }

        VoxelSize(std::array<float, 3> xyz_) : xyz(xyz_) {}

        VoxelSize& operator=(std::array<float, 3> xyz_) {
            xyz = xyz_;
            return *this;
        }

        /// Cast to the underlying Vec3D datatype
        operator dalaran::datatypes::Vec3D() const {
            return xyz;
        }
    };
} // namespace dalaran::components

namespace dalaran {
    static_assert(sizeof(dalaran::datatypes::Vec3D) == sizeof(components::VoxelSize));

    /// \private
    template <>
    struct Loggable<components::VoxelSize> {
        static constexpr std::string_view ComponentType = "dalaran.components.VoxelSize";

        /// Returns the arrow data type this type corresponds to.
        static const std::shared_ptr<arrow::DataType>& arrow_datatype() {
            return Loggable<dalaran::datatypes::Vec3D>::arrow_datatype();
        }

        /// Serializes an array of `dalaran::components::VoxelSize` into an arrow array.
        static Result<std::shared_ptr<arrow::Array>> to_arrow(
            const components::VoxelSize* instances, size_t num_instances
        ) {
            if (num_instances == 0) {
                return Loggable<dalaran::datatypes::Vec3D>::to_arrow(nullptr, 0);
            } else if (instances == nullptr) {
                return dalaran::Error(
                    ErrorCode::UnexpectedNullArgument,
                    "Passed array instances is null when num_elements> 0."
                );
            } else {
                return Loggable<dalaran::datatypes::Vec3D>::to_arrow(&instances->xyz, num_instances);
            }
        }
    };
} // namespace dalaran
