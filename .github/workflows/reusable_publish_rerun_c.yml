name: Build and Publish C/C++ SDKs

on:
  workflow_call:
    inputs:
      concurrency:
        type: string
        required: true
      release-version:
        description: "Release Version Number (Must match Cargo.toml)"
        type: string
        required: true
      release-commit:
        description: "Which commit to build+publish"
        type: string
        required: true

permissions:
  contents: "read"
  id-token: "write"
  packages: "read"

jobs:
  linux-arm64:
    name: "Linux-Arm64"
    uses: ./.github/workflows/reusable_build_and_upload_dalaran_c.yml
    with:
      CONCURRENCY: publish-dalaran-c-linux-arm64-${{ github.ref_name }}
      PLATFORM: linux-arm64
      RELEASE_COMMIT: ${{ inputs.release-commit }}
    secrets: inherit

  linux-x64:
    name: "Linux-x64"
    uses: ./.github/workflows/reusable_build_and_upload_dalaran_c.yml
    with:
      CONCURRENCY: publish-dalaran-c-linux-x64-${{ github.ref_name }}
      PLATFORM: linux-x64
      RELEASE_COMMIT: ${{ inputs.release-commit }}
    secrets: inherit

  macos-arm64:
    name: "Mac-Arm"
    uses: ./.github/workflows/reusable_build_and_upload_dalaran_c.yml
    with:
      CONCURRENCY: publish-dalaran-c-macos-arm64-${{ github.ref_name }}
      PLATFORM: macos-arm64
      RELEASE_COMMIT: ${{ inputs.release-commit }}
    secrets: inherit

  windows-x64:
    name: "Windows-x64"
    uses: ./.github/workflows/reusable_build_and_upload_dalaran_c.yml
    with:
      CONCURRENCY: publish-dalaran-c-windows-${{ github.ref_name }}
      PLATFORM: windows-x64
      RELEASE_COMMIT: ${{ inputs.release-commit }}
    secrets: inherit

  bundle-and-upload-dalaran_cpp:
    name: "Bundle and upload dalaran_cpp_sdk.zip"
    needs: [linux-arm64, linux-x64, macos-arm64, windows-x64]
    uses: ./.github/workflows/reusable_bundle_and_upload_dalaran_cpp.yml
    with:
      CONCURRENCY: bundle-dalaran-c-${{ github.ref_name }}
      RELEASE_COMMIT: ${{ inputs.release-commit }}
    secrets: inherit
