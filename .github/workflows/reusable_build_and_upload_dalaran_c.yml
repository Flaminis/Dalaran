name: Reusable Dalaran-c Build

on:
  workflow_call:
    inputs:
      CONCURRENCY:
        required: true
        type: string
      PLATFORM:
        required: true
        type: string
      ADHOC_NAME:
        required: false
        type: string
        default: ""
      RELEASE_COMMIT:
        required: false
        type: string
        default: ""

  workflow_dispatch:
    inputs:
      ADHOC_NAME:
        required: false
        type: string
        description: "Name of the adhoc build, used for upload directory"
        default: ""
      PLATFORM:
        type: choice
        options:
          - linux-arm64
          - linux-x64
          - windows-x64
          - macos-arm64
        description: "Platform to build for"
        required: true
      CONCURRENCY:
        required: false
        type: string
        default: "adhoc"
        description: "Concurrency group to use"

concurrency:
  group: ${{ inputs.CONCURRENCY }}-build-dalaran_c
  cancel-in-progress: true

env:
  RUSTFLAGS: --deny warnings
  RUSTDOCFLAGS: --deny warnings

defaults:
  run:
    shell: bash --noprofile --norc -euo pipefail {0}

permissions:
  contents: "read"
  packages: "read"
  id-token: "write"

jobs:
  set-config:
    name: Set Config (${{ inputs.PLATFORM }})
    runs-on: ubuntu-latest-16-cores
    outputs:
      RUNNER: ${{ steps.set-config.outputs.runner }}
      TARGET: ${{ steps.set-config.outputs.target }}
      CONTAINER: ${{ steps.set-config.outputs.container }}
      LIB_NAME: ${{ steps.set-config.outputs.lib_name }}
    steps:
      - name: Set runner and target based on platform
        id: set-config
        run: |
          case "${{ inputs.PLATFORM }}" in
            linux-arm64)
              runner="ubuntu-arm-16-core"
              target="aarch64-unknown-linux-gnu"
              container="'ghcr.io/rerun-io/ci_docker:0.18.0'"
              lib_name="libdalaran_c.a"
              ;;
            linux-x64)
              runner="ubuntu-latest-16-cores"
              target="x86_64-unknown-linux-gnu"
              container="'ghcr.io/rerun-io/ci_docker:0.18.0'"
              lib_name="libdalaran_c.a"
              ;;
            windows-x64)
              runner="windows-latest-8-cores"
              target="x86_64-pc-windows-msvc"
              container="null"
              lib_name="dalaran_c.lib"
              ;;
            macos-arm64)
              runner="macos-26" # Small runners, because building dalaran_c is fast
              target="aarch64-apple-darwin"
              container="null"
              lib_name="libdalaran_c.a"
              ;;
            *) echo "Invalid platform" && exit 1 ;;
          esac
          echo "runner=$runner" >> "$GITHUB_OUTPUT"
          echo "target=$target" >> "$GITHUB_OUTPUT"
          echo "container=$container" >> "$GITHUB_OUTPUT"
          echo "lib_name=$lib_name" >> "$GITHUB_OUTPUT"

  rs-build-dalaran_c:
    name: Build dalaran_c (${{ needs.set-config.outputs.RUNNER }})
    timeout-minutes: 60

    needs: [set-config]

    runs-on: ${{ needs.set-config.outputs.RUNNER }}
    container:
      image: ${{ fromJson(needs.set-config.outputs.CONTAINER) }}
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Show context
        run: |
          echo "GITHUB_CONTEXT": $GITHUB_CONTEXT
          echo "JOB_CONTEXT": $JOB_CONTEXT
          echo "INPUTS_CONTEXT": $INPUTS_CONTEXT
          echo "ENV_CONTEXT": $ENV_CONTEXT
        env:
          ENV_CONTEXT: ${{ toJson(env) }}
          GITHUB_CONTEXT: ${{ toJson(github) }}
          JOB_CONTEXT: ${{ toJson(job) }}
          INPUTS_CONTEXT: ${{ toJson(inputs) }}

      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.RELEASE_COMMIT || ((github.event_name == 'pull_request' && github.event.pull_request.head.ref) || '') }}

      - uses: prefix-dev/setup-pixi@v0.10.0
        with:
          pixi-version: v0.71.3

      - name: Set up Rust and Authenticate to GCS
        uses: ./.github/actions/setup-rust
        with:
          cache_key: "build-${{ inputs.PLATFORM }}"
          save_cache: false
          workload_identity_provider: ${{ secrets.GOOGLE_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GOOGLE_SERVICE_ACCOUNT }}
          targets: ${{ needs.set-config.outputs.TARGET }}

      - name: Build dalaran_c (release)
        run: pixi run cargo build --locked -p dalaran_c --release --target ${{ needs.set-config.outputs.TARGET }}

      - name: Get sha
        id: get-sha
        run: |
          full_commit="${{ inputs.RELEASE_COMMIT || ((github.event_name == 'pull_request' && github.event.pull_request.head.sha) || github.sha) }}"
          echo "sha=$(echo $full_commit | cut -c1-7)" >> "$GITHUB_OUTPUT"

      - name: "Upload dalaran_c (commit)"
        uses: google-github-actions/upload-cloud-storage@v3
        with:
          path: "./target/${{ needs.set-config.outputs.TARGET }}/release/${{ needs.set-config.outputs.LIB_NAME }}"
          destination: "dalaran-builds/commit/${{ steps.get-sha.outputs.sha }}/dalaran_c/${{ inputs.PLATFORM }}"
          parent: false
          process_gcloudignore: false

      - name: "Upload dalaran_c (adhoc)"
        if: ${{ inputs.ADHOC_NAME != '' }}
        uses: google-github-actions/upload-cloud-storage@v3
        with:
          path: "./target/${{ needs.set-config.outputs.TARGET }}/release/${{ needs.set-config.outputs.LIB_NAME }}"
          destination: "dalaran-builds/adhoc/${{inputs.ADHOC_NAME}}/dalaran_c/${{ inputs.PLATFORM }}"
          parent: false
          process_gcloudignore: false
