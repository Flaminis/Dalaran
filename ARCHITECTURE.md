# Dalaran architecture

This document describes the technical architecture of Dalaran.

## See also

-   [`BUILD.md`](BUILD.md)
-   [`TESTING.md`](TESTING.md)
-   [`dalaran_py/README.md`](dalaran_py/README.md) - build instructions for Python SDK
-   [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md)
-   [`CODE_STYLE.md`](CODE_STYLE.md)
-   [`CONTRIBUTING.md`](CONTRIBUTING.md)
-   [`RELEASES.md`](RELEASES.md)

## The major components

### Logging APIs

It all starts with logging. You can log rich data (point clouds, images, etc.) with either our Python SDK or our Rust SDK.

The logging SDK:s encodes the data using Apache Arrow (see more below).

The logging data can be written to disk as `.dlr` files, or transmitted over gRPC to either a Dalaran Viewer or a Dalaran Server.

### Dalaran Viewer

The Dalaran Viewer is where log data is visualized. It is usually run as a native app, but can also be compiled to WebAssembly (Wasm) and run in a browser.

#### Native Viewer

The easiest way to launch the Viewer is directly from the logging API with `rr.init("dalaran_example_app", spawn=True)`. However, the standalone Viewer can also be run from the command line, for example to view an `.dlr` file: `dalaran mydata.dlr`.

#### Web viewer

You can try running the Viewer in a browser using `rr.serve()` in Python, or using `dalaran --web-viewer mydata.dlr`.

The web viewer consists of just a few small files - a thin `.html`, a `.wasm` blob, and an auto-generated `.js` bridge for the wasm. These files are served using the [`dl_web_viewer_server`](https://github.com/Flaminis/Dalaran/tree/latest/crates/viewer/dl_web_viewer_server) crate.

The web viewer can load `.dlr` files (just drag-drop them into the browser), or read logging data streamed over gRPC.

### `.dlr` files

`.dlr` ("**R**e**r**un **D**ata") is just a bunch of log messages appended one after the other to a file.

NOTE: `.dlr` files do not yet offer full backwards or forwards compatibility. However, the current version of Dalaran will always be able to open `.dlr` files generated by the *previous* version. This is not guaranteed to work for files generated with older versions.

## Technologies we use

### Apache Arrow

[Apache Arrow](https://arrow.apache.org/) is a language-independent columnar memory format for arbitrary data. We use it to encode the log data when transmitting it over the network or storing it in an `.dlr` file. We also use it in our in-RAM data store, [`dl_chunk_store`](crates/store/dl_chunk_store/README.md).

In Rust, we use the [`arrow` crate](https://crates.io/crates/arrow).

### `wgpu`

The Dalaran Viewer uses the [`wgpu`](https://github.com/gfx-rs/wgpu) graphics API. It provides a high-performance abstraction over Vulkan, Metal, D3D12, D3D11, OpenGLES, WebGL and [WebGPU](https://en.wikipedia.org/wiki/WebGPU). This lets us write the same code graphics code for native as for web.

On web builds, we use WebGPU when available on the Web, but automatically fall back to a WebGL based emulation layer (with a more limited feature set).

We have written our own high-level rendering crate on top of `wgpu`, called [`dl_renderer`](crates/viewer/dl_renderer/README.md).

### `egui`

The GUI in the Dalaran Viewer is using [`egui`](https://www.egui.rs/), a cross-platform, [immediate mode GUI](https://github.com/emilk/egui#why-immediate-mode).

We use [`eframe`](https://github.com/emilk/egui/tree/master/crates/eframe), the egui framework, to run `egui` on both native and web.

### Wasm

Wasm (short for [WebAssembly](https://webassembly.org/)) is a binary instruction format supported by all major browser.
The Dalaran Viewer can be compiled to Wasm and run in a browser.

Threading support in Wasm is nascent, so care must be taken that we don't spawn any threads when compiling for `wasm32`.

Wasm has no access to the host system, except via JS calls (something that may change once [WASI](https://wasi.dev/) rolls out), so when compiling for `wasm32` you can NOT use the Rust standard library to:

-   Access files
-   Read environment variables
-   Get the current time (use [`instant`](https://crates.io/crates/instant) instead)
-   Use networking (use [`ehttp`](https://github.com/emilk/ehttp), [`reqwest`](https://github.com/seanmonstar/reqwest), or [`ewebsock`](https://github.com/rerun-io/ewebsock) instead)
-   etc

## Immediate mode

The Dalaran Viewer uses an [immediate mode GUI](https://github.com/emilk/egui#why-immediate-mode), [`egui`](https://www.egui.rs/). This means that each frame the entire GUI is being laid out from scratch.

In fact, the whole of the Dalaran Viewer is written in an immediate mode style. Each rendered frame it will query the in-RAM data store, massage the results, and feed it to the renderer.

The advantage of immediate mode is that is removes all state management. There is no callbacks that are called when some state has already changed, and the state of the blueprint is always in sync with what you see on screen.

Immediate mode is also a forcing function, forcing us to relentlessly optimize our code.
This leads to a very responsive GUI, where there is no "hiccups" when switching data source or doing time scrubbing.

Of course, this will only take us so far. In the future we plan on caching queries and work submitted to the renderer so that we don't perform unnecessary work each frame. We also plan on doing larger operation in background threads. This will be necessary in order to support viewing large datasets, e.g. several million points. The plan is still to do so within an immediate mode framework, retaining most of the advantages of stateless code.

## Crates

Here is an overview of the crates included in the project:

<picture>
  <img src="https://static.rerun.io/architecture_diagram/6e0cc663abd216f92fdf0e3e988832f05fd197a7/full.png" alt="">
  <source media="(max-width: 480px)" srcset="https://static.rerun.io/architecture_diagram/6e0cc663abd216f92fdf0e3e988832f05fd197a7/480w.png">
  <source media="(max-width: 768px)" srcset="https://static.rerun.io/architecture_diagram/6e0cc663abd216f92fdf0e3e988832f05fd197a7/768w.png">
  <source media="(max-width: 1024px)" srcset="https://static.rerun.io/architecture_diagram/6e0cc663abd216f92fdf0e3e988832f05fd197a7/1024w.png">
  <source media="(max-width: 1200px)" srcset="https://static.rerun.io/architecture_diagram/6e0cc663abd216f92fdf0e3e988832f05fd197a7/1200w.png">
</picture>

<!-- !!! IMPORTANT!!!

This image must be updated each time a crate is added/removed/updated.

FigJam document: https://www.figma.com/file/Umob8ztK1HmYKLUMSq8aPb/Crates-org

Update instructions:
1) Update the FigJam document
2) Select all -> right-click -> Copy as PNG
3) `pixi run upload-image --name architecture_diagram`
4) Copy/paste the resulting HTML
-->

### SDK/CLI/Wasm top-level crates

| Crate     | Description                          |
| --------- | ------------------------------------ |
| dalaran-cli | Dalaran native CLI binary crate        |
| Dalaran     | Dalaran Rust SDK and Viewer shim crate |
| dalaran_c   | Dalaran C SDK                          |
| dalaran_py  | Dalaran Python SDK                     |
| dl_sdk    | Dalaran logging SDK                    |

### Viewer crates

##### UI crates

| Crate                  | Description                                                                                                |
| ---------------------- |------------------------------------------------------------------------------------------------------------|
| dl_arrow_ui            | Show arrow data in a tree of dalaran list_items and format arrow with syntax highlighting.                   |
| dl_blueprint_tree      | The UI for the blueprint tree in the left panel.                                                           |
| dl_chunk_store_ui      | A chunk store browser UI.                                                                                  |
| dl_component_fallbacks | Provides default fallbacks for Dalaran components in the fallback provider registry.                         |
| dl_component_ui        | Provides UI editors for Dalaran component data for registration with the Dalaran Viewer component UI registry. |
| dl_dataframe_ui        | Rich table widget over DataFusion.                                                                         |
| dl_gamepad             | Gamepad input handling for the Dalaran viewer.                                                               |
| dl_memory_view         | Flamegraph visualization for memory usage trees                                                            |
| dl_plot                | Plot ui components shared between time series and bar chart view.                                          |
| dl_recording_panel     | The UI for the recording panel.                                                                            |
| dl_redap_browser       | The UI and communication to implement the in-viewer redap server browser.                                  |
| dl_selection_panel     | The UI for the selection panel.                                                                            |
| dl_time_panel          | The time panel of the Dalaran Viewer, allowing to control the displayed timeline & time.                     |
| dl_time_ruler          | Time ruler widget shared by the time panel and time-aware views.                                           |
| dl_view                | Types & utilities for defining View classes and communicating with the Viewport.                           |
| dl_view_bar_chart      | A View that shows a single bar chart.                                                                      |
| dl_view_dataframe      | A View that shows the data contained in entities in a table.                                               |
| dl_view_graph          | A View that shows a graph (node-link diagram).                                                             |
| dl_view_map            | A View that shows geospatial data on a map.                                                                |
| dl_view_spatial        | Views that show entities in a 2D or 3D spatial relationship.                                               |
| dl_view_state_timeline | A view that shows state transitions as horizontal lanes over time.                                         |
| dl_view_tensor         | A View dedicated to visualizing tensors with arbitrary dimensionality.                                     |
| dl_view_text_document  | A simple View that shows a single text box.                                                                |
| dl_view_text_log       | A View that shows text entries in a table and scrolls with the active time.                                |
| dl_view_time_series    | A View that shows plots over Dalaran timelines.                                                              |
| dl_viewer              | The Dalaran Viewer                                                                                           |
| dl_viewer_mcp          | MCP server that allows agents to interact with the viewer.                                                 |
| dl_viewport            | The central viewport panel of the Dalaran viewer.                                                            |

##### UI support crates

| Crate                 | Description                                                          |
| --------------------- | -------------------------------------------------------------------- |
| dl_context_menu       | Support crate for context menu and actions.                          |
| dl_data_ui            | Provides UI elements for Dalaran component data for the Dalaran Viewer.  |
| dl_renderer           | A wgpu-based renderer for all your visualization needs.              |
| dl_renderer_examples  | Examples for dl_renderer                                             |
| dl_ui                 | Dalaran GUI theme and helpers, built around egui                       |
| dl_viewer_context     | Dalaran Viewer state that is shared with the viewer's code components. |
| dl_viewport_blueprint | The data model description of the viewport panel.                    |

### Application-level store

| Crate           | Description                                                     |
| --------------- | --------------------------------------------------------------- |
| dl_dataframe    | The Dalaran public data APIs.                                     |
| dl_datafusion   | DataFusion interfaces to Dalaran gRPC queries                     |
| dl_entity_db    | In-memory storage of Dalaran entities                             |
| dl_log_encoding | Helpers for encoding and transporting Dalaran log messages        |
| dl_protos       | Dalaran remote store gRPC API types                               |
| dl_query        | Querying data in the dl_chunk_store                             |
| dl_server       | In-memory data server                                           |
| dl_tf           | Dealing with spatial transforms & transform frames              |
| dl_sdk_types    | The built-in Dalaran data types, component types, and archetypes. |

### Low-level store

| Crate          | Description                                                                                   |
| -------------- | --------------------------------------------------------------------------------------------- |
| dl_chunk       | A chunk of Dalaran data, encoded using Arrow. Used for logging, transport, storage and compute. |
| dl_chunk_store | An in-memory time series database for Dalaran log data, based on Apache Arrow.                  |
| dl_log_types   | The basic building blocks of the Dalaran data types and tables.                                 |
| dl_sorbet      | Dalaran arrow metadata and record batch definitions.                                            |
| dl_types_core  | The core traits and types that power Dalaran's data model.                                      |

### Data flow

| Crate                | Description                                                       |
| -------------------- | ----------------------------------------------------------------- |
| dl_redap_client      | Official client for the Dalaran Data Protocol                       |
| dl_redap_tests       | Official test suite for the Dalaran Data Protocol                   |
| dl_importer          | Handles importing of Dalaran data from file using importer plugins  |
| dl_parquet           | Parquet file loading with configurable column grouping            |
| dl_hdf5              | HDF5 file loading into Dalaran chunks                               |
| dl_mp4_reader        | Mp4 video file loading into Dalaran chunks                          |
| dl_data_source       | Handles loading of Dalaran data from different sources              |
| dl_grpc_client       | Client for the legacy StoreHub API                                |
| dl_grpc_server       | Server for the legacy StoreHub API                                |
| dl_web_viewer_server | Serves the Dalaran web viewer (Wasm and HTML) over HTTP             |

### Build support

| Crate                    | Description                                                          |
| ------------------------ | -------------------------------------------------------------------- |
| dl_build_info            | Information about the build. Use together with dl_build_tools        |
| dl_build_tools           | build.rs helpers for generating build info                           |
| dl_dev_tools             | Various tools for Dalaran development. Each tool has a subcommand.     |
| dl_protos_builder        | Generates code for Dalaran remote store gRPC API                       |
| dl_types_builder         | Generates code for Dalaran's SDKs from flatbuffers definitions.        |
| dl_types_builder_macros  | The `#[dalaran_type]` attribute macro used by Dalaran's IDL definitions. |
| dl_types_builder_prelude | The vocabulary that Dalaran's IDL definitions are written against.     |
| dl_web_tests             | Discovers and runs browser-based Wasm tests.                         |

### Utilities

| Crate                | Description                                                                                   |
| -------------------- | --------------------------------------------------------------------------------------------- |
| dl_analytics         | Dalaran's analytics SDK                                                                         |
| dl_async             | Async runtime abstractions for native and WebAssembly targets.                                |
| dl_arrow_util        | Helpers for working with arrow                                                                |
| dl_auth              | Authentication and authorization helpers                                                      |
| dl_backoff           | Simple backoff logic used for retries.                                                        |
| dl_byte_size         | Calculate the heap-allocated size of values at runtime                                        |
| dl_byte_size_derive  | Derive macro for `dl_byte_size::SizeBytes`                                                    |
| dl_capabilities      | Capability tokens                                                                             |
| dl_case              | Case conversions, the way Dalaran likes them                                                    |
| dl_crash_handler     | Detect panics and signals, logging them and optionally sending them to analytics.             |
| dl_error             | Helpers for handling errors.                                                                  |
| dl_format            | Miscellaneous tools to format and parse numbers, durations, etc.                              |
| dl_grpc_headers      | Dalaran gRPC header conventions (`x-dalaran-*` consts, version interceptor, tower layers).        |
| dl_int               | Small numeric helper traits: saturating integer casts and unsigned absolute value.            |
| dl_lenses            | A collection of lenses for mapping component data to Dalaran semantic types.                    |
| dl_lenses_core       | Core lens types and composable Arrow array transformations                                    |
| dl_log               | Helpers for setting up and doing text logging in the Dalaran crates.                            |
| dl_log_channel       | An in-memory channel of Dalaran data messages                                                   |
| dl_mcap              | Convert MCAP into Dalaran-compatible data.                                                      |
| dl_memory            | Run-time memory tracking and profiling.                                                       |
| dl_mutex             | A wrapper around `parking_lot::Mutex` useful for debugging deadlocks.                         |
| dl_perf_telemetry    | In and out of process performance profiling utilities for Dalaran & Redap                       |
| dl_quota_channel     | A mpsc channel that applies backpressure based on byte size                                   |
| dl_ros_msg           | Parsing and deserializing ROS   messages                                                      |
| dl_rvl               | Codecs for depth compression including RVL and PNG.                                           |
| dl_span              | An integer range that always has a non-negative length                                        |
| dl_string_interner   | Yet another string interning library                                                          |
| dl_test_mocks        | In-process server doubles (`MockOtlpCollector`, `MockPostHog`) for OTel/PostHog test capture. |
| dl_tracing           | Helpers for tracing/spans/flamegraphs and such.                                               |
| dl_tuid              | 128-bit Time-based Unique Identifier                                                          |
| dl_uri               | Parsing and constructing of Dalaran URIs                                                        |
| dl_video             | Video decoding library                                                                        |
| dl_web               | Utilities for interacting with Web APIs                                                       |

### Test crates

| Crate            | Description                                                   |
| ---------------- | ------------------------------------------------------------- |
| dl_test_context  | Provides a test context that builds on `dl_viewer_context.    |
| dl_test_viewport | Extends the `dl_test_context` with viewport-related features. |

### Deprecated crates

| Crate    | Description                |
| -------- | -------------------------- |
| dl_types | Old name of `dl_sdk_types` |

### Dependencies and docs

In order to get a dependency graph for our in-house crates and their docs, we recommend you run:

```
cargo install cargo-depgraph
cargo depgraph --all-deps --workspace-only --all-features --dedup-transitive-deps | dot -Tpng > /tmp/dalaran-deps.png
open /tmp/dalaran-deps.png
```

and:

```
cargo doc --no-deps --open
```

and then browse through the `re_*` crates.
