namespace dalaran.blueprint.archetypes;


/// Configuration for the background of a spatial view.
table Background (
  "attr.python.aliases": "datatypes.Rgba32Like | blueprint_components.BackgroundKindLike",
  "attr.dalaran.scope": "blueprint"
) {
  // --- Required ---

  /// The type of the background.
  kind: dalaran.blueprint.components.BackgroundKind ("attr.dalaran.component_required", order: 1000);

  // --- Optional ---

  /// Color used for the solid background type.
  // TODO(andreas): Can't link to [components.BackgroundKind.SolidColor] since blueprint components aren't part of the doc page yet.
  color: dalaran.components.Color ("attr.dalaran.component_optional", nullable, order: 2000);
}

/// Configuration of a background in a plot view.
table PlotBackground (
  "attr.dalaran.scope": "blueprint"
) {
  /// Color used for the background.
  color: dalaran.components.Color ("attr.dalaran.component_optional", nullable, order: 1000);

  /// Should the grid be drawn?
  show_grid: dalaran.blueprint.components.Enabled ("attr.dalaran.component_optional", nullable, order: 2000);
}

/// Configuration of a background in a graph view.
table GraphBackground (
  "attr.dalaran.scope": "blueprint"
) {
  /// Color used for the background.
  color: dalaran.components.Color ("attr.dalaran.component_optional", nullable, order: 1000);
}
