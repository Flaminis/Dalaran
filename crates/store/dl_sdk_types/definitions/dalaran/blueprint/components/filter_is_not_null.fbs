
namespace dalaran.blueprint.components;

/// Configuration for the filter is not null feature of the dataframe view.
table FilterIsNotNull (
  "attr.arrow.transparent",
  "attr.rust.derive": "Default, PartialEq, Eq",
  "attr.rust.repr": "transparent",
  "attr.dalaran.scope": "blueprint"
) {
    filter_is_not_null: dalaran.blueprint.datatypes.FilterIsNotNull (order: 100);
}
