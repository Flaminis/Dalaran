namespace dalaran.blueprint.components;

// ---

/// Whether the view should auto-scroll to follow the time cursor.
struct AutoScroll (
  "attr.arrow.transparent",
  "attr.dalaran.scope": "blueprint",
  "attr.python.aliases": "bool",
  "attr.rust.derive": "Copy, Default, PartialEq, Eq, PartialOrd, Ord",
  "attr.rust.repr": "transparent",
  "attr.rust.tuple_struct"
) {
  auto_scroll: dalaran.datatypes.Bool (order: 100);
}
