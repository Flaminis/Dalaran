namespace dalaran.blueprint.components;

// ---

/// A timeline column in a text log table.
table TimelineColumn (
  "attr.arrow.transparent",
  "attr.rust.derive": "PartialEq, Eq, Default, Hash",
  "attr.dalaran.scope": "blueprint",
  "attr.rust.repr": "transparent",
  "attr.rust.tuple_struct"
) {
  /// The timeline column.
  timeline_column: dalaran.blueprint.datatypes.TimelineColumn (order: 100);
}

