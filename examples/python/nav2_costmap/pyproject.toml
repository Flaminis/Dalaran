[project]
name = "nav2_costmap"
version = "0.1.0"
readme = "README.md"
dependencies = ["numpy", "dalaran-sdk"]

[project.scripts]
nav2_costmap = "nav2_costmap:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
