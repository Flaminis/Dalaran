---
title: "`dalaran mcap info` output changed"
hidden: true
type: breaking
---

### `dalaran mcap info` output changed

The `dalaran mcap info` CLI command has been rewritten to output richer and more detailed file-level information instead of just diagnostic checks.
The diagnostic checks are now in a dedicated `dalaran mcap check` subcommand instead.
